/**
 * An isogram is a word that with no repeating letters (consecutive or non-consecutive).
 * Implement a function that determines if a given string is an isogram.
 * - empty strings are an isogram
 * - ignore letter casing
 * @param {string} testString - String to test for isogram
 * @returns {boolean}
 */
function isIsogram(testString) {
  // todo: implement isIsogram function
}

module.exports.isIsogram = isIsogram;
